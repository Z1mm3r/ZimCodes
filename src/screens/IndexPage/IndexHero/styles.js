import {percent, vw} from 'utils/utils.js'

const styles = {
    heroContainer:{
        width: `${percent(100)}`,
    },
    heroText:{
        fontSize: vw(10),
        whiteSpace: "nowrap"

    },
}

export default styles