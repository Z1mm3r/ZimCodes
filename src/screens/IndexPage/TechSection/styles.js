import {em, percent} from 'utils/utils.js'

const styles = {
    heroContainer:{
        width: percent(100),
    },
    heroText:{
        fontSize: em(10)
    },
    lightContainer:{
    },
    darkContainer:{
    }
}

export default styles