const styles = {
    lightModeButton: {
        root:{
            color: 'black'
        }
    },
    darkModeButton: {
        root:{
            color: 'white'
        }
    }
}

export default styles